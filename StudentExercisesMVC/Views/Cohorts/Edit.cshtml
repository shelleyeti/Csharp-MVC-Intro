@model StudentExercises.Models.ViewModels.CohortEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Cohort Edit</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Cohort.Name" class="control-label"></label>
                @Html.TextBoxFor(model => model.Cohort.Name, new { @class = "form-control" })
                @*<input asp-for="Name" value="@Html.DisplayNameFor(model => model.Name)" class="form-control" />*@
                <span asp-validation-for="Cohort.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
